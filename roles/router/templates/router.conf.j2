{% set wan = ansible_facts['ansible_local']['vpp']['wan_interface'] %}

set dhcp client intfc {{ wan }}
set interface state {{ wan }} up

bvi create instance 0
set interface l2 bridge bvi0 1 bvi
set interface ip address bvi0 10.0.0.1/24
set interface state bvi0 up

create tap id 0 host-ip4-addr 10.0.0.2/24 host-ip4-gw 10.0.0.1
set interface l2 bridge tap0 1
set interface state tap0 up

nat44 enable
nat44 add interface address {{ wan }}
set interface nat44 in bvi0 out {{ wan }}

nat44 add static mapping tcp local 10.0.0.2 22 external {{ wan }} 22
